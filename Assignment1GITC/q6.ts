// Adding white space in the name using tab
var personName = "  Maryam Nadeem   ";
console.log("Person name with whitespace: " );
console.log(personName);
// Removing whitespace from the variable usin .trim()
var strippedName = personName.trim()
console.log("Name after stripping whitespace: ");
console.log(strippedName);